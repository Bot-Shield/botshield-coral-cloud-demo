import { LightningElement } from 'lwc';

export default class SiteHeader extends LightningElement {
    // Booking flows through the floating cart (c-coral-cart, fixed top-right)
    // and the experience picker below — the header carries nav only.
}
